<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.karin.snowflakewake" >


    <!-- boot permission-->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".AlarmListActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AlarmDetailActivity"
            android:label="@string/title_activity_alarm_detail" >
        </activity>

        <!-- Make the alarms stay even if phone is turned off-->
        <receiver android:name="com.example.karin.snowflakewake.AlarmManagerHelper">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service android:name="com.example.karin.snowflakewake.AlarmService" android:enabled="true" />

        <activity android:name="com.example.karin.snowflakewake.AlarmScreen" />

    </application>


</manifest>

